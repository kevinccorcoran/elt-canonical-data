# docker/dbt-docs-canonical/.env.docs — template (NO SECRETS). Copy and fill in.
#   cp .env.example .env.docs   # then edit
# The .env.docs file is gitignored; only this template is tracked.
# The rest of this container's DB config (DB_HOST/DB_PORT/DB_USER/DB_DATABASE)
# is set inline in docker-compose.yml; only the password comes from here.

DB_PASSWORD=changeme
